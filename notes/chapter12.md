When implementing the composite patter, what interface should we implement to our collection of objects (leaf elements)?

[HFDP]

---

Remember, the composite needs to implement the same interface as the leaf elements.